const initialState = {
  status: 'NOT CONNECTED',
  user: {},
  error: null,
};

const user = (state = initialState, action) => {
  switch (action.type) {
    default: return state;
  }
};

export default user;
